{
    "version": 2,
    "projectSettings": {
        "buildCommand": "bun run build",
        "outputDirectory": "dist",
        "installCommand": "bun install"
    },
    "buildCommand": "bun run build",
    "outputDirectory": "dist",
    "installCommand": "bun install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}